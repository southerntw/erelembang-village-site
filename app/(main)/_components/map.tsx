import { ComposableMap, Geographies, Geography } from "react-simple-maps";

export const Map = () => {
  return (
    <div className="w-500 h-500 flex flex-col items-center justify-center bg-orange-500">
      map
    </div>
  );
};
